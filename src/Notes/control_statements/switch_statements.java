package Notes.control_statements;

public class switch_statements {
    public static void main(String[] args) {

    }
}

/*

* Switch statements *

it can have one or n numbers of cases
break is optional to use
case value must be of expression type only. can be literals or constants it doesn't allow variables.
every case should be unique otherwise it will give compile-time error

* syntax *

switch(exp)
{
    case 1:
    break
    case 2;
    break
    default:

}

nesting is allowed
 */
